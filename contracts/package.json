{
  "name": "@timberos/data-console-contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared runtime-validated schema + TypeScript types for timberOS Data Console telemetry. Single source of truth for the JSON the mod emits.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "demo": "tsx scripts/consume.mts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
